[pytest]
# Run from the backend/ directory.
# `testpaths` tells pytest where to look for tests.
testpaths = tests

# Add the backend root to sys.path so `from app.xxx import yyy` resolves.
pythonpath = .

# Show short tracebacks and live output
addopts = -v --tb=short